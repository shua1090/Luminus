class Tree{
    Tree left;
    Tree right;
    int value;
    func free(int value, Tree nice, Tree lice)->void{
        value = 5;
        int cool = 9;
        nice.left.free(5, nice, nice);
    }
}

func nice()->void{
    Tree t;
}