function multiplyByTwo(int * num, int addTimes) returns int{
    int ctr = 0;
    while (ctr < addTimes) {
        printf("Num is currently %d\n", *num);
        *num = *num * 2;
        ctr = ctr + 1;
    }
    return 0;
}